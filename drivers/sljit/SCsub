Import('env')
Export('env');

env.add_source_files(env.drivers_sources,"sljit_src/sljitLir.c")

env.Append(CPPFLAGS=['-DSLJIT_CONFIG_AUTO=1'])
if env['target'] == 'release':
	env.Append(CPPFLAGS=['-DSLJIT_DEBUG=0'])
